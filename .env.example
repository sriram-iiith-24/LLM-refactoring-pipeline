# ============================================
# API KEYS (DO NOT COMMIT - USE GITHUB SECRETS)
# ============================================

# Gemini Keys - will be injected by GitHub Actions
GEMINI_KEY_1=your_gemini_key_here
GEMINI_KEY_2=your_second_gemini_key_here

# GitHub
GITHUB_TOKEN=your_github_token_here
GITHUB_REPO=owner/repository-name

# Local repo path - for GitHub Actions runner
LOCAL_REPO_PATH=/github/workspace/code/repo-clone

# ============================================
# FILE DISCOVERY SETTINGS
# ============================================

# Scanning mode: 'large', 'all', or 'manual'
# - large: Only files with >= SCAN_MIN_LINES (recommended for production)
# - all: Process all Java files in the repository
# - manual: Only files specified in MANUAL_FILES
SCAN_MODE=large

# For 'manual' mode: comma-separated file paths (relative to repo root)
MANUAL_FILES=app/src/main/java/org/example/SomeFile.java

# For 'large' mode: minimum lines threshold
SCAN_MIN_LINES=300

# Maximum files to process per run (prevents API quota exhaustion)
MAX_FILES_PER_RUN=20
